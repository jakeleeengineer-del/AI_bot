# EngTutor Frontend

React + Vite mobile-first chat UI.

## Setup

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8000`. Override it with:

```bash
VITE_API_BASE=http://localhost:8000 npm run dev
```
